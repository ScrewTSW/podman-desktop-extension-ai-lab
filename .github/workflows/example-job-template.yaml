name: example-job-template

on:
  workflow_call:
    inputs:
      trigger-workflow-run-id:
        required: true
        type: string

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Was called
        run: echo "Example job was called by ${{ github.event.inputs.trigger-workflow-run-id }}"
